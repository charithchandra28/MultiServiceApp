import 'package:flutter/material.dart';


// Placeholder screens for respective services
class GroceryBookingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Grocery Booking')),
      body: Center(child: Text('Grocery Booking Screen')),
    );
  }
}