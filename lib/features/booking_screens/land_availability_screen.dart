import 'package:flutter/material.dart';
// TODO Implement this library.


class LandAvailabilityScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Land Availability')),
      body: Center(child: Text('Land Availability Screen')),
    );
  }
}