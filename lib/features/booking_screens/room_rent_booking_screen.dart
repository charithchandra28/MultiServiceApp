import 'package:flutter/material.dart';

class RoomRentBookingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Room Rent Booking')),
      body: Center(child: Text('Room Rent Booking Screen')),
    );
  }
}
