import 'package:flutter/material.dart';


class HairSalonBookingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Hair Salon Booking')),
      body: Center(child: Text('Hair Salon Booking Screen')),
    );
  }
}